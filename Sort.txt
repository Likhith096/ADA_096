MERGE SORT :-

2 functions 

MergeSort()
if(l<h){
    int m = l+(h-l)/2;
    MergeSort(A,l,m);
    MergeSort(A,m+1,h);
    Merge(A,l,h)
}

Merge()
{
    int i=l,j=h,k=l;
    int B[100]; //Temp array
    int m = l+(h-l)/2;
    while(i<=m && j <=h)
        if(A[i] < A[j])   B[k++] = A[i]; .....
    while(i<=m)
        B[k++] = A[i];
    ....
    for(..)
        A[i] = B[i]; //Resubstituing the array
}

HEAPSORT:-
3 functions : swap , heapify -> (Max heap conversion) , heapsort


